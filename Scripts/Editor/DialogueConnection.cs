public class DialogueConnection
{
    public DialogueNode fromNode;
    public DialogueNode toNode;

    public DialogueConnection(DialogueNode from, DialogueNode to)
    {
        fromNode = from;
        toNode = to;
    }
}